query GetBillInfoByBillId($billId: Int!) {
  allBillInfos(condition: {idBill: $billId}) {
    edges {
      node {
        id
        idProduct
        count
        idBill
        unitPrice
        totalPrice
        productByIdProduct {
          id
          name
          price
        }
      }
    }
  }
}
