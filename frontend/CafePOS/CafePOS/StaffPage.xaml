<Page
    x:Class="CafePOS.StaffPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CafePOS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="0" Margin="0,0,0,10">
            <Button Content="➕ Thêm nhân viên" Click="AddStaffButton_Click"/>
        </StackPanel>

        <Grid Grid.Row="1" Background="Transparent" Margin="10,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Tên" Grid.Column="0" FontWeight="Bold" Padding="10" VerticalAlignment="Center"/>
            <TextBlock Text="Chức vụ" Grid.Column="1" FontWeight="Bold" Padding="10" VerticalAlignment="Center"/>
            <TextBlock Text="SĐT" Grid.Column="2" FontWeight="Bold" Padding="10" VerticalAlignment="Center"/>
            <TextBlock Text="Email" Grid.Column="3" FontWeight="Bold" Padding="10" VerticalAlignment="Center"/>
            <TextBlock Text="Ngày sinh" Grid.Column="4" FontWeight="Bold" Padding="10" VerticalAlignment="Center"/>
            <TextBlock Text="Lương" Grid.Column="5" FontWeight="Bold" Padding="10" VerticalAlignment="Center"/>
        </Grid>


        <ListView x:Name="StaffListView" Grid.Row="2" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" Margin="0,0,0,10" Background="White" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Name}" Grid.Column="0"
                                   TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Position}" Grid.Column="1" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Phone}" Grid.Column="2" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Email}" Grid.Column="3"
                                   TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Dob}" Grid.Column="4" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SalaryFormatted}" Grid.Column="5" VerticalAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" Grid.Column="6" Spacing="10" VerticalAlignment="Center">
                            <Button Content="✏️" Click="EditStaff_Click" DataContext="{Binding}"/>
                            <Button Content="🗑️" Click="DeleteStaff_Click" DataContext="{Binding}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ContentDialog
            x:Name="StaffDialog"
            Title="Thông tin nhân viên"
            PrimaryButtonText="Lưu"
            SecondaryButtonText="Hủy"
            PrimaryButtonClick="SaveStaffDialog_Click"
            SecondaryButtonClick="CancelStaffDialog_Click">

            <StackPanel Spacing="10" Width="300">
                <TextBox x:Name="NameTextBox" PlaceholderText="Họ tên"/>
                <DatePicker x:Name="DobDatePicker"/>
                <ComboBox x:Name="GenderComboBox" PlaceholderText="Giới tính">
                    <ComboBoxItem Content="Nam"/>
                    <ComboBoxItem Content="Nữ"/>
                    <ComboBoxItem Content="Khác"/>
                </ComboBox>
                <TextBox x:Name="PhoneTextBox" PlaceholderText="Điện thoại"/>
                <TextBox x:Name="EmailTextBox" PlaceholderText="Email"/>
                <TextBox x:Name="PositionTextBox" PlaceholderText="Chức vụ"/>
                <TextBox x:Name="SalaryTextBox" PlaceholderText="Lương (VNĐ)"/>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>
