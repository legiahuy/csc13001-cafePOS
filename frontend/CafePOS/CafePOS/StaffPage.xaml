<Page
    x:Class="CafePOS.StaffPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CafePOS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#F5F5F5">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="0" Margin="0,0,0,10">
            <Button Content="➕ Thêm nhân viên" Click="AddStaffButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Grid Background="#EFEFEF" BorderBrush="#E0E0E0" BorderThickness="1" Margin="10,0,10,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Tên" Grid.Column="0" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                <TextBlock Text="Chức vụ" Grid.Column="1" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                <TextBlock Text="SĐT" Grid.Column="2" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                <TextBlock Text="Email" Grid.Column="3" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                <TextBlock Text="Ngày sinh" Grid.Column="4" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                <TextBlock Text="Lương" Grid.Column="5" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                <TextBlock Text="Thao tác" Grid.Column="6" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
            </Grid>

            <ListView x:Name="StaffListView"
                      Margin="10,0,10,10"
                      SelectionMode="None"
                      Background="#F9F9F9"
                      BorderThickness="1"
                      BorderBrush="#E0E0E0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="5" Background="White" Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Name}" Grid.Column="0" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Position}" Grid.Column="1" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Phone}" Grid.Column="2" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Email}" Grid.Column="3" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Dob}" Grid.Column="4" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding SalaryFormatted}" Grid.Column="5" HorizontalAlignment="Center"/>

                            <StackPanel Orientation="Horizontal" Grid.Column="6" HorizontalAlignment="Center" Spacing="10">
                                <Button Content="✏️" Click="EditStaff_Click" DataContext="{Binding}"/>
                                <Button Content="🗑️" Click="DeleteStaff_Click" DataContext="{Binding}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <ContentDialog
            x:Name="StaffDialog"
            Title="Thông tin nhân viên"
            PrimaryButtonText="Lưu"
            SecondaryButtonText="Hủy"
            PrimaryButtonClick="SaveStaffDialog_Click"
            SecondaryButtonClick="CancelStaffDialog_Click">

            <ScrollViewer MaxHeight="500" VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="10" Width="300">
                    <TextBox x:Name="NameTextBox" PlaceholderText="Họ tên"/>
                    <DatePicker x:Name="DobDatePicker"/>
                    <ComboBox x:Name="GenderComboBox" PlaceholderText="Giới tính">
                        <ComboBoxItem Content="Nam"/>
                        <ComboBoxItem Content="Nữ"/>
                        <ComboBoxItem Content="Khác"/>
                    </ComboBox>
                    <TextBox x:Name="PhoneTextBox" PlaceholderText="Điện thoại"/>
                    <TextBox x:Name="EmailTextBox" PlaceholderText="Email"/>
                    <TextBox x:Name="PositionTextBox" PlaceholderText="Chức vụ"/>
                    <TextBox x:Name="SalaryTextBox" PlaceholderText="Lương (VNĐ)"/>
                    <TextBlock Text="Tên đăng nhập (Username)" Margin="0,10,0,0"/>
                    <TextBox x:Name="UsernameTextBox" PlaceholderText="Nhập tên đăng nhập"/>
                    <TextBlock Text="Mật khẩu" Margin="0,10,0,0"/>
                    <PasswordBox x:Name="PasswordBox" PlaceholderText="Nhập mật khẩu"/>
                </StackPanel>
            </ScrollViewer>
        </ContentDialog>

    </Grid>
</Page>
